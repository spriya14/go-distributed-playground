apiVersion: v1
kind: Service
metadata:
  name: rpc-toy-service
spec:
  selector:
    app: rpc-toy
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      nodePort: 30007
  type: NodePort
